# ====================================================================
#             Environment Variables for SIGANTENG Backend
# ====================================================================
# Salin file ini ke .env dan isi dengan kredensial Anda.
# --------------------------------------------------------------------

# --- Core Application Settings ---
# Atur ke "development" atau "production"
APP_ENV=development
# URL Frontend untuk konfigurasi CORS (bisa lebih dari satu, pisahkan dengan koma)
BACKEND_CORS_ORIGINS=http://localhost:3000

# --- Database (Neon PostgreSQL) ---
# Dapatkan dari dashboard Neon Anda.
DATABASE_URL=postgres://user:password@host:port/dbname

# --- Background Jobs & Caching (Render Redis) ---
# Dapatkan dari dashboard Render Anda setelah membuat layanan Redis.
REDIS_URL=redis://user:password@host:port

# --- Authentication (Clerk) ---
# Dapatkan dari dashboard Clerk Anda.
CLERK_SECRET_KEY=sk_test_...

# --- File Storage (Cloudinary) ---
# Dapatkan dari dashboard Cloudinary Anda.
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# --- AI Service Keys ---
# Kunci API untuk layanan OpenAI (GPT, Whisper, dll.)
OPENAI_API_KEY=sk-...
# Token untuk mengakses model dari Hugging Face Hub
HF_API_TOKEN=hf_...

# --- DEFAULT AI PROVIDERS ---
# Tentukan provider default untuk setiap layanan.
# Bisa "openai" atau "huggingface" (tergantung konfigurasi Anda)
DEFAULT_LLM_PROVIDER=openai
DEFAULT_VISION_PROVIDER=openai
DEFAULT_STT_PROVIDER=openai
DEFAULT_TTS_PROVIDER=openai
DEFAULT_EMBEDDING_MODEL=all-MiniLM-L6-v2 # Model sentence-transformers untuk embeddings

# --- Pinecone (untuk digunakan nanti di Fase D) ---
PINECONE_API_KEY=your_pinecone_api_key_here


# ====================================================================
#             Environment Variables for SIGANTENG Frontend
# ====================================================================
# Variabel di bawah ini perlu disalin ke file .env.local di
# direktori /frontend.
# --------------------------------------------------------------------

# --- Clerk Publishable Key (Public) ---
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...

# --- Backend API URL (Public) ---
# Gunakan URL deployment Render Anda untuk production.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
